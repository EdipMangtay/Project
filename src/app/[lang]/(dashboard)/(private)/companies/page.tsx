import { Typography } from '@mui/material'

import { getMetadata } from '@/utils/getMetadata'

export const metadata = getMetadata('Şirketler')

const Companies = () => {
  return <Typography variant='h5'>Şirketler</Typography>
}

export default Companies
